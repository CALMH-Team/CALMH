import { IonicGestureConfig } from './ionic-gesture-config';

describe('IonicGestureConfig', () => {
  it('should create an instance', () => {
    expect(new IonicGestureConfig()).toBeTruthy();
  });
});
